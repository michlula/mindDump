{
  "framework": null,
  "buildCommand": "cd dashboard && npm install && npm run build",
  "outputDirectory": "dashboard/dist",
  "installCommand": "npm install && cd server && npm install",
  "functions": {
    "api/*.ts": {
      "includeFiles": "server/**"
    }
  },
  "crons": [{ "path": "/api/flush", "schedule": "* * * * *" }]
}
